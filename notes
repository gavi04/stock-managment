stock items 
--stock name 
--hsn code(identification code of product) (dropdown)
--size 
--length 
--*item code( autogenrated can be overwritten)(unique)
--group(raw material,work in progress,finished goods)
--gst rate (percentage)
--sale rate (int upto 2 points)
--uom (unit of measurement) (dropdown)
--opening stock quantity (int upto 3 decimal point )
--purchase rate (int upto 2 decimal point )
--size differnece (can be  +ve or negative upto 2 decimal places)
--batch no 
--General description
--opening stock date
--isi mark(bool) (default no)

--------------------------------------------------------------------------

Party master ( type to  be removed)
--Gst no( 15 digits all capiatl no and letters)
--Party name 
--Party code
--Address 
--city 
--district
--state
--pincode
--mobile no

--------------------------------------------------------------

Purchase Vocher
--Input feilds
----date
----customer (to be choosen from the popup with search bar)
----invoice number
----batch no.
----Expiry date
----vehicle no 
----bilty no
----vocher no autogenerated


----pcs
----quantity( input feilds)
----base rate( int upto two decimals)

-- older table nmmping
----item (code to be choosen)
----stock name( fetch older but could be updated)
----hsn(fetch no update)
----size diff( fetch can be updated)
----net rate(calculate base rate + size diff ( but can be edited))
----taxable value(pcs/quantity * net rate)( but can be  edited)
----gst( auto calculated but can be updated)
----broker(choosen from party master)
----remarks
----total to be displayed